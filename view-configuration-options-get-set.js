// Get view native configuration option (here e.g. visibility)
this.context.options._metadata.visibility.get("value");

// Set view native configuration option (here e.g. visibility)
this.context.options._metadata.visibility.set("value", newValue);

// Get view custom configuration option
this.context.options.myOption.get("value");

// Set view custom configuration option
this.context.options.myOption.set("value", newValue);

// Other important binding and configuration options here: 
// https://www.ibm.com/docs/en/baw/19.x?topic=views-binding-data-configuration-options
